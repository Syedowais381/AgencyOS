import { ModulePlaceholder } from "@/components/module-placeholder";

export default function AiAgentsPage() {
  return (
    <ModulePlaceholder
      title="AI agents"
      description="Content ideation, packaging, and competitor-aware assistants grounded in onboarding + performance data."
      phase={2}
    />
  );
}
