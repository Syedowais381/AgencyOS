import { ModulePlaceholder } from "@/components/module-placeholder";

export default function ClientsPage() {
  return (
    <ModulePlaceholder
      title="Students / Clients"
      description="Client subaccounts, onboarding progress, and lifecycle analytics with strict RLS isolation."
      phase={1}
    />
  );
}
