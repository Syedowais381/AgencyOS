import { ModulePlaceholder } from "@/components/module-placeholder";

export default function SalesPage() {
  return (
    <ModulePlaceholder
      title="Sales analytics"
      description="Setter and closer leaderboards, pipeline velocity, and revenue attribution — Phase 1 lays the dashboard shell; deeper sales modules follow."
      phase={1}
    />
  );
}
