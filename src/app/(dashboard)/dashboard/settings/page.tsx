import { ModulePlaceholder } from "@/components/module-placeholder";

export default function SettingsPage() {
  return (
    <ModulePlaceholder
      title="Settings"
      description="Workspace preferences, agency branding, API keys (server-side), and notification channels."
      phase={1}
    />
  );
}
