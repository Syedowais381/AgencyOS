import { ModulePlaceholder } from "@/components/module-placeholder";

export default function TasksPage() {
  return (
    <ModulePlaceholder
      title="Tasks"
      description="Operational tasking with assignments, SLAs, and notifications — pairs with the content workflow board."
      phase={3}
    />
  );
}
